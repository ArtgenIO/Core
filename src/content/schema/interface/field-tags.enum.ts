export enum FieldTag {
  PRIMARY = 'primary',
  CREATED = 'created',
  UPDATED = 'updated',
  DELETED = 'deleted',
  VERSION = 'version',
  NULLABLE = 'nullable',
  INDEX = 'index',
  UNIQUE = 'unique',
}
