export enum FieldTag {
  PRIMARY = 'primary',
  CREATED = 'created',
  UPDATED = 'updated',
  DELETED = 'deleted',
  VERSION = 'version',
  TAGS = 'tags',
  NULLABLE = 'nullable',
  INDEX = 'index',
  UNIQUE = 'unique',
}
